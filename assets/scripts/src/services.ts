const page = document.querySelector("#schedules-services");

if (page) {
  const titulo = page.querySelector("h2") as HTMLHeadingElement;
  const formulario = page.querySelector("form") as HTMLFormElement;
  const data = page.querySelector("h3") as HTMLHeadingElement;
  const options = page.querySelector(".options") as HTMLDivElement;
  const input = page.querySelector(".options label input") as HTMLInputElement;
}
